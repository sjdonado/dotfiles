## 0. Authorize the acceptance budget before writing instruction text

- [x] 0.1 Put the batch to the user before editing `agents/AGENTS.md`: this change stales the existing `bench/runs/effectiveness-v1` evidence, so validation needs a new batch of 18 paid sessions (9 cases on 2 models, per design.md), while `default-yolo-and-session-tasks` tasks 4.1, 4.3, 4.4, 4.9 and `improve-harness-effectiveness` tasks 3.5, 3.6, 4.4 are still open against the same budget. Verify by recording the user's answer, one combined batch, this change alone, or ship unvalidated and say so in the PR, in the branch note before any session runs. Answered 2026-09-18: the paid batch is not authorized. The change ships unvalidated behaviorally, the pull request body says so and names the 18-session batch that would validate it, task 4.2 stays open, and no model session ran.

## 1. State publication authority once in the shared instructions

- [x] 1.1 Add a publication authority section to `agents/AGENTS.md` defining the agreement-shaped gate (settled purpose, whole scope, acceptance evidence, identifiable source of user agreement) and the exclusion that an agent-authored ledger, a passing check run, or a finished diff is not that source. Verify the section exists and that `grep` finds no second surface still stating an entry gate in terms of artifact presence alone.
- [x] 1.2 In the same section, name `yolo` as the only workflow exception to separate publication confirmation, scoped to the task branch and its own pull request, and state that issues, tracker comments, top-level pull request comments, and review replies stay outside it. Verify by reading the section against spec requirement "Yolo is the only scoped publication exception".
- [x] 1.3 In the same section, list what does not authorize an external write (an earlier approved pull request, default-branch hygiene, a workflow's own procedure, an unrelated open or blocked pull request, a note recording past approval) and state that an existing specific authorization stays valid and is not re-asked. Verify each item in the list maps to a scenario in the delta spec.
- [x] 1.4 Rewrite the parts of "Approval means autonomous execution" and "External communication" that currently imply authority so they defer to the new section instead of restating it, and re-derive "One line of work is one branch and one PR" from agreement rather than branch hygiene. Verify by diffing those sections and confirming no remaining sentence grants an external write on its own.
- [x] 1.5 State the in-flight overlap rule from design.md: an approved run finishes its authorized scope including its remote checks, incoming scope waits for the first local feedback batch after the run's terminal state, and a contradicted premise escalates instead. Verify the rule appears once and contradicts nothing in `yolo` or `feedback`.

## 2. Point the skills at the section and remove their local variants

- [x] 2.1 Rewrite `agents/skills/yolo/SKILL.md`'s entry paragraph to state the agreement gate and to qualify each artifact input by agreement state, so a handoff, ledger, or change directory the user never accepted is `proto`'s input. Verify the skill no longer lists an input that can satisfy entry on presence alone.
- [x] 2.2 Change `agents/skills/feedback/SKILL.md` so a full round ends locally: apply, run the local ladder, report, and ask once whether to publish this batch. On approval, commit, push the same branch, refresh that pull request's title and body, and drive its required checks with no further confirmation. Verify step 6's unconditional push is gone and the deferred-round versus full-round distinction still resolves without contradiction.
- [x] 2.3 Reconcile `agents/skills/proto/SKILL.md` with the section: promotion hands the diff and ledger to `yolo` only once user agreement exists, and the ledger alone never promotes. Verify the promotion-timing conflict recorded in the branch note is gone.
- [x] 2.4 Confirm `agents/skills/address-review/SKILL.md` and `agents/skills/create-ticket/SKILL.md` already prepare content before the external act and only add a pointer to the new section where they restate authority. Verify by diff that no new gate was added to either.
- [x] 2.5 Search for stale variants across `agents/AGENTS.md` and every skill (`grep -rn "push\|open a PR\|external write\|approval"` under `agents/`) and remove or redirect each contradicting sentence. Verify the search output contains no remaining statement that authorizes publication outside the new section.

## 2b. Give intent a home and read it by path

- [x] 2b.1 In `agents/AGENTS.md` Continuity, define the Contract head: Purpose, End state (with acceptance evidence), Key tasks (task list path); written at the first checkpoint of any editing workflow, filled from request and repository evidence before asking, refreshed before the next edit when a human interaction changes purpose or end state. Verify the section states all three lines and the refresh trigger.
- [x] 2b.2 Point `proto` setup step 3, `yolo` step 1, `handoff`, `verification`'s contract input, and `adversarial-review` Reviewer A at that block by path; Reviewer A's requirements source is named in priority order (change specs and tasks, else Contract block, else ticket thread), verbatim. Verify each skill names the path and none says "summarize the requirements".
- [x] 2b.3 Add the handoff recommendation trigger to Continuity (two intent changes in a session, or a compaction) and remove `disable-model-invocation: true` from `agents/skills/handoff/SKILL.md` or document why it must stay and add the recommendation path instead. Verify the trigger appears once and the handoff skill can be recommended by the agent.

## 2c. Delegate by default

- [x] 2c.1 Rewrite `agents/AGENTS.md` Orchestrating: no offer; `verification` runs one tier down under `yolo` unless the project records otherwise; reviewers stay at the orchestrator tier; waiting is never delegated. Verify `agents/AGENTS.md:188-192`'s offer text is gone and `yolo` step 5 and 6 reference the default.
- [x] 2c.2 Record this repository's preference in root `AGENTS.md`: mechanical stretches go to a subagent one tier below the orchestrator. Verify the line exists and names no model.
- [x] 2c.3 Add the fork iteration to `agents/skills/proto/SKILL.md`, gated on a written End state, one lower-tier subagent per approach in its own worktree, compared against the End state, nothing committed. Verify the paragraph states the gate and the no-commit rule.
- [x] 2c.4 Fold self-grill step 2 into `openspec-propose` and `yolo` step 1 as an inline evidence step, and delete the standalone grill-me prescription at `agents/AGENTS.md:72`. Verify `grill-me` remains invocable by name and is no longer prescribed on every plan.

## 2d. Unblock landing and tidy notes

- [x] 2d.1 In `agents/skills/land/SKILL.md`, open tasks that are paid behavioral acceptance do not block archive: record them as unverified in the archived change and the note, then archive. Verify step 3 and 4 state the rule.
- [x] 2d.2 `land` moves the merged note to `.agent/archive/` instead of preserving it in place; Continuity states no note is written for the default branch. Verify `land` step 6 and `agents/AGENTS.md` Continuity carry both.
- [x] 2d.3 Fix the two known conflicts: `agents/skills/proto/SKILL.md:21` "second iteration" becomes "second step"; `agents/skills/feedback/SKILL.md:26`'s first-round question is removed in favor of the local-by-default round from 2.2. Verify by grep.

## 3. Make the new behavior observable in bench

- [x] 3.0 Scope provenance per case in `bench/measure` and `bench/scenarios.py`: each case declares the skills it routes through and the `agents/AGENTS.md` sections it depends on; the input hash covers those plus the runner and fixtures; `verify --behavior` prints the hashed surfaces per case. Verify with `bench/measure verify --local` and an offline check that editing an unlisted section leaves a case's hash unchanged while editing a listed one changes it.

- [x] 3.1 Add the five new cases to `bench/scenarios.py`: mixed settled and unsettled scope, an agent-authored contract offered as agreement, precedent offered as authorization for another pull request, a first feedback round that must not push, and an approved batch that must push to the same pull request without re-asking. Declare each case's expected artifacts and assertions in the case before running anything, and verify `bench/measure verify --local` passes.
- [x] 3.2 Give each new case a remote and the `bench/fixture_gh.py` forge substitute, and assert Git and forge side effects (commit present or absent, push present or absent, pull request count, comment calls), not transcript wording alone. Verify each case offline against a simulated compliant run and a simulated violating run, in both directions, per the task 4.9 lesson.
- [x] 3.3 Record the regression subset in `bench/README.md`: the four existing cases this change's surfaces touch (`route-bounded`, `route-open-shape`, the external communication case, and `handoff-v1` on Luna as the fixed first trial), and the resulting 18-session count. Verify the documented count matches what `bench/measure verify --behavior --regression --batch <new-dir>` reports as missing. Adversarial review corrected the subset twice during implementation: `feedback`, `land-open`, `land-merged`, `recoverable` and `unrecoverable` all declare skills this change rewrites, so they belong in it, which moves the count from design.md's 18 sessions to 27 rows and 28 sessions, still inside the 32-session cap. Recorded in the pull request body.

## 4. Validate

- [x] 4.1 Run `bench/measure verify --local` and `openspec validate settle-workflow-routing-contract --strict`. Verify both exit zero. Done 2026-09-18 through the `verification` skill in a subagent one tier down: `bench/measure verify --local` green once the untracked `agents/skills/synced/` whitespace hits are filtered out, `python3 bench/test_measure.py` green, `openspec validate settle-workflow-routing-contract --strict` green, `git diff --check` green, and `bench/measure verify --behavior --regression --batch <probe>` exits nonzero with all 17 rows `missing` and a `surfaces` list on each.
- [ ] 4.2 Run the batch authorized in task 0.1 with `bench/measure verify --behavior --run --batch <new-dir>`, review each transcript, and write its `review.json`. Verify no case is recorded as passing without its transcript review, and record an unauthorized or unavailable check as unverified.
- [x] 4.3 Report what the runs actually showed, including any instruction a model did not follow and any case whose oracle could not discriminate. Verify the report names each failing case rather than an aggregate rate; a green local check is not acceptance. Reported 2026-09-18: no run happened, because task 0.1's batch was not authorized. There is no failing case and no passing case; every row of the regression subset is `missing`. The pull request body states that the change ships behaviorally unvalidated and names the 18-session batch that would validate it.

## 5. Ship

- [x] 5.1 Through `yolo`, approved 2026-09-18 for a subagent at the tier below the orchestrator: implement sections 1 through 3 on one branch, run `adversarial-review` on the accumulated diff, and open one pull request. Never merge.
- [x] 5.2 State in the pull request body what is validated and what is not, which decisions were made without asking (the in-flight overlap rule and the 18-session batch shape), and the competing unarchived changes. Verify the body names the batch authorization actually given.
- [x] 5.3 Fold this section's items into this change's `tasks.md` as the single task list for the work, and remove the `Routing and publication contract` section from `.agent/docs%2Fhandoff-destination.tasks.md`, preserving the unrelated sections above it. Verify the branch task list no longer duplicates any item here.
