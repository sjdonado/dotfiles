## 0. Establish portable project guidance

- [x] 0.1 Bootstrap root AGENTS.md from actual repository structure and existing commands, clearly separating it from agents/AGENTS.md and marking behavioral acceptance pending until the new entry point exists. Preserve nested and linked instruction scope.
- [x] 0.2 Create the self-contained harness-boostrap skill using skill-creator, with read-only audit and explicitly requested setup/update behavior, evidence-backed output, no mandatory personal-workflow dependencies, and no installer changes.
- [x] 0.3 Audit the bootstrapped root document with harness-boostrap and correct grounded gaps as part of this approved implementation. Document the final verification entry point only after it exists.

## 1. Repair continuation

- [x] 1.1 Record the current instruction word count and preserve the existing failed handoff artifacts as the baseline; use the same branch and exclude unrelated edits.
- [x] 1.2 Make proto's existing checkpoint create or refresh the branch note before presentation, including deferred requirements, rationale, checks, and next action.
- [x] 1.3 Clarify missing-note recovery versus missing-contract clarification in AGENTS.md and align yolo input handling with actual user approval.
- [x] 1.4 Preserve handoff-v1 prompts and independent expectations; reserve its Luna run as the first matrix trial after final instruction and fixture versions are frozen.

## 2. Close workflow transitions

- [x] 2.1 Reconcile feedback and address-review PR-description updates with AGENTS.md after authorized pushes, retaining review-reply approval and deferred check debt.
- [x] 2.2 Let land verify merge and close the existing note without requiring OpenSpec, while retaining verification and archive behavior when a change exists.
- [x] 2.3 Remove superseded instructions and duplicated policy; retain shared rules plus short checkpoint references, with no increase in total words across pre-existing changed instruction files. Report the new optional skill and project instructions separately.

## 3. Check distinct outcomes

- [x] 3.1 Extend bench/measure minimally with independently seeded missing-contract cases, read-only plain-language routing, feedback PR synchronization, and open/merged no-OpenSpec landing. Keep real external writes unavailable to these fixtures.
- [x] 3.2 Add harness-boostrap audit and setup/repeat fixtures on ordinary repositories, checking command grounding, preservation of scoped instructions, no personal-harness dependencies, fresh-session command discovery, and idempotence.
- [x] 3.3 Implement bench/measure verify --local and verify --behavior, with nonzero behavioral acceptance for failures, missing coverage, or stale evidence. Record matching input provenance and budget usage so later passes reuse only valid results. Map these commands in root AGENTS.md and bench/README.md.
- [x] 3.4 Define independent expected outcomes before model execution: correct implementation, correct clarification without dependent edits, read-only behavior, current PR metadata, or awaiting-merge/completed note state as appropriate. Add focused offline checks for new runner logic.
- [ ] 3.5 Freeze the instruction and fixture versions, then run the design's 26-session maximum matrix, including the first trial and portable project-guidance cases, with exact model/settings and scenario provenance. Preserve all attempts; do not substitute models or discard failures.
- [ ] 3.6 Inspect transcript evidence for skill routing, note consumption, unnecessary rereads, and false completion. Report fixture pass/fail separately from model turn completion and list untested surfaces.

## 4. Validate and hand off

- [x] 4.1 Run python3 -m py_compile bench/measure bench/test_measure.py, python3 bench/test_measure.py, git diff --check, and openspec validate improve-harness-effectiveness --strict; resolve failures caused by this change.
- [x] 4.2 Validate harness-boostrap frontmatter with skill-creator's validator, verify the final project-local commands work, and confirm that a deliberate missing/failing behavioral result cannot yield overall acceptance.
- [x] 4.3 Document correctness results and per-model usage/tool observations without claiming universal effectiveness or token savings from unmatched or failed runs.
- [ ] 4.4 Through yolo, audit the accumulated branch diff, run its adversarial review, preserve unrelated work, and open or update the same branch's PR with resolved checks green. If smoke acceptance or the session budget blocks completion, report the evidence rather than claim effectiveness. Never merge.
