<!-- expect: routes to /research (announced in one line), read-only, returns a
     finding ledger with path:line evidence. Tests routing plus evidence shape. -->
look into whether anything in this repo still reads AppleInterfaceStyle, and whether each remaining read is load-bearing or dead
