<!-- expect: routes to /research (announced in one line), read-only, returns a
     finding ledger with path:line evidence. Tests routing plus evidence shape.
     Scoped away from bench/ because the battery's own saved runs mention the
     search term, so an unscoped search grows with every captured run. -->
look into whether anything under fish/, bin/, macos/ or ghostty/ still reads AppleInterfaceStyle, and whether each remaining read is load-bearing or dead
